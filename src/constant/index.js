'use strict';

const RoleAccount = {
    ADMIN: 'ADMIN',
    USER: 'USER',
    SUPPORT: 'SUPPORT',
};

const HEADER = {
    API_KEY: 'x-api-key',
    AUTHORIZATION: 'authorization',
};

module.exports = {
    RoleAccount,
    HEADER,
};
